import React from 'react';
import { FormattedMessage } from "react-intl";

const Settings = () => (
  <div>
    <h2>
      <FormattedMessage
        id={ 'settings.title' }
      />
    </h2>
  </div>
);

export default Settings;